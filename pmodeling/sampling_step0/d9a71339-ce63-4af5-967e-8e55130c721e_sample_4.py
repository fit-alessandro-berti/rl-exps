import pm4py
from pm4py.objects.powl.obj import StrictPartialOrder, OperatorPOWL, Transition, SilentTransition
from pm4py.objects.process_tree.obj import Operator
A = Transition(label='Site Survey')
B = Transition(label='Structural Check')
C = Transition(label='Modular Install')
D = Transition(label='Hydroponic Setup')
E = Transition(label='Nutrient Mix')
F = Transition(label='Sensor Setup')
G = Transition(label='AI Training')
H = Transition(label='Data Capture')
I = Transition(label='Maintenance Plan')
J = Transition(label='Pest Scan')
K = Transition(label='Growth Monitor')
L = Transition(label='Harvest Sync')
M = Transition(label='Quality Test')
N = Transition(label='Package Prep')
O = Transition(label='Logistics Plan')
skip = SilentTransition()
site_survey = OperatorPOWL(operator=Operator.LOOP, children=[A])
structural_check = OperatorPOWL(operator=Operator.LOOP, children=[B])
modular_install = OperatorPOWL(operator=Operator.LOOP, children=[C])
hydroponic_setup = OperatorPOWL(operator=Operator.LOOP, children=[D])
nutrient_mix = OperatorPOWL(operator=Operator.LOOP, children=[E])
sensor_setup = OperatorPOWL(operator=Operator.LOOP, children=[F])
ai_training = OperatorPOWL(operator=Operator.LOOP, children=[G])
data_capture = OperatorPOWL(operator=Operator.LOOP, children=[H])
maintenance_plan = OperatorPOWL(operator=Operator.LOOP, children=[I])
pest_scan = OperatorPOWL(operator=Operator.LOOP, children=[J])
growth_monitor = OperatorPOWL(operator=Operator.LOOP, children=[K])
harvest_sync = OperatorPOWL(operator=Operator.LOOP, children=[L])
quality_test = OperatorPOWL(operator=Operator.LOOP, children=[M])
package_prep = OperatorPOWL(operator=Operator.LOOP, children=[N])
logistics_plan = OperatorPOWL(operator=Operator.LOOP, children=[O])
root = StrictPartialOrder(nodes=[site_survey, structural_check, modular_install, hydroponic_setup, nutrient_mix, sensor_setup, ai_training, data_capture, maintenance_plan, pest_scan, growth_monitor, harvest_sync, quality_test, package_prep, logistics_plan])
root.order.add_edge(site_survey, structural_check)
root.order.add_edge(structural_check, modular_install)
root.order.add_edge(modular_install, hydroponic_setup)
root.order.add_edge(hydroponic_setup, nutrient_mix)
root.order.add_edge(nutrient_mix, sensor_setup)
root.order.add_edge(sensor_setup, ai_training)
root.order.add_edge(ai_training, data_capture)
root.order.add_edge(data_capture, maintenance_plan)
root.order.add_edge(maintenance_plan, pest_scan)
root.order.add_edge(pest_scan, growth_monitor)
root.order.add_edge(growth_monitor, harvest_sync)
root.order.add_edge(harvest_sync, quality_test)
root.order.add_edge(quality_test, package_prep)
root.order.add_edge(package_prep, logistics_plan)