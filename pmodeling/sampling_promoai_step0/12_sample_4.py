A = Transition(label='Adjust compensation')
B = Transition(label='Approve promotion')
C = Transition(label='Conducts formal performance review')
D = Transition(label='Consider employee for promotion or new role')
E = Transition(label='Create personal development plan')
F = Transition(label='Identify development needs or career aspirations')
G = Transition(label='Receive feedback and evaluation from supervisors')
H = Transition(label='Set new responsibilities')
I = Transition(label='Transition into new role')
J = Transition(label='Work on skill enhancement')
skip = SilentTransition()
loop = OperatorPOWL(operator=Operator.LOOP, children=[J, G])
xor = OperatorPOWL(operator=Operator.XOR, children=[H, skip])
root = StrictPartialOrder(nodes=[loop, xor])
root.order.add_edge(loop, xor)
root.order.add_edge(loop, E)
root.order.add_edge(E, F)
root.order.add_edge(F, G)
root.order.add_edge(G, loop)
root.order.add_edge(E, H)
root.order.add_edge(H, I)
root.order.add_edge(H, B)
root.order.add_edge(B, I)
root.order.add_edge(I, A)