root = StrictPartialOrder(
    nodes=[
        Transition(label='Site Survey'),
        Transition(label='Design Layout'),
        Transition(label='Install Modules'),
        Transition(label='Set Controls'),
        Transition(label='Select Crops'),
        Transition(label='Configure Irrigation'),
        Transition(label='Setup Lighting'),
        Transition(label='Deploy Sensors'),
        Transition(label='Train Staff'),
        Transition(label='Start Cultivation'),
        Transition(label='Monitor Growth'),
        Transition(label='Manage Pests'),
        Transition(label='Harvest Crops'),
        Transition(label='Pack Produce'),
        Transition(label='Distribute Goods'),
    ],
    order=[
        (Transition(label='Site Survey'), Transition(label='Design Layout')),
        (Transition(label='Design Layout'), Transition(label='Install Modules')),
        (Transition(label='Install Modules'), Transition(label='Set Controls')),
        (Transition(label='Set Controls'), Transition(label='Select Crops')),
        (Transition(label='Select Crops'), Transition(label='Configure Irrigation')),
        (Transition(label='Configure Irrigation'), Transition(label='Setup Lighting')),
        (Transition(label='Setup Lighting'), Transition(label='Deploy Sensors')),
        (Transition(label='Deploy Sensors'), Transition(label='Train Staff')),
        (Transition(label='Train Staff'), Transition(label='Start Cultivation')),
        (Transition(label='Start Cultivation'), Transition(label='Monitor Growth')),
        (Transition(label='Monitor Growth'), Transition(label='Manage Pests')),
        (Transition(label='Manage Pests'), Transition(label='Harvest Crops')),
        (Transition(label='Harvest Crops'), Transition(label='Pack Produce')),
        (Transition(label='Pack Produce'), Transition(label='Distribute Goods')),
    ]
)