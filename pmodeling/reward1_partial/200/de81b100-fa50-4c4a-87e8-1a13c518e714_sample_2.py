import pm4py
from pm4py.objects.powl.obj import StrictPartialOrder, OperatorPOWL, Transition, SilentTransition
from pm4py.objects.process_tree.obj import Operator

site_survey = Transition(label='Site Survey')
permit_acquire = Transition(label='Permit Acquire')
rack_design = Transition(label='Rack Design')
seed_selection = Transition(label='Seed Selection')
nutrient_mix = Transition(label='Nutrient Mix')
lighting_setup = Transition(label='Lighting Setup')
sensor_install = Transition(label='Sensor Install')
system_test = Transition(label='System Test')
staff_hire = Transition(label='Staff Hire')
training_lead = Transition(label='Training Lead')
waste_manage = Transition(label='Waste Manage')
supply_chain = Transition(label='Supply Chain')
crop_cycle = Transition(label='Crop Cycle')
data_monitor = Transition(label='Data Monitor')
harvest_plan = Transition(label='Harvest Plan')
distribution = Transition(label='Distribution')

skip = SilentTransition()
xor1 = OperatorPOWL(operator=Operator.XOR, children=[site_survey, permit_acquire])
xor2 = OperatorPOWL(operator=Operator.XOR, children=[rack_design, seed_selection])
xor3 = OperatorPOWL(operator=Operator.XOR, children=[nutrient_mix, lighting_setup])
xor4 = OperatorPOWL(operator=Operator.XOR, children=[sensor_install, system_test])
xor5 = OperatorPOWL(operator=Operator.XOR, children=[staff_hire, training_lead])
xor6 = OperatorPOWL(operator=Operator.XOR, children=[waste_manage, supply_chain])
xor7 = OperatorPOWL(operator=Operator.XOR, children=[crop_cycle, data_monitor])
xor8 = OperatorPOWL(operator=Operator.XOR, children=[harvest_plan, distribution])

root = StrictPartialOrder(nodes=[xor1, xor2, xor3, xor4, xor5, xor6, xor7, xor8])
root.order.add_edge(xor1, xor2)
root.order.add_edge(xor1, xor3)
root.order.add_edge(xor2, xor4)
root.order.add_edge(xor2, xor5)
root.order.add_edge(xor3, xor6)
root.order.add_edge(xor3, xor7)
root.order.add_edge(xor4, xor8)
root.order.add_edge(xor5, xor8)
root.order.add_edge(xor6, xor8)
root.order.add_edge(xor7, xor8)