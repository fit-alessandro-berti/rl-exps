import pm4py
from pm4py.objects.powl.obj import StrictPartialOrder, OperatorPOWL, Transition, SilentTransition
from pm4py.objects.process_tree.obj import Operator

client_consult = Transition(label='Client Consult')
spec_analysis = Transition(label='Spec Analysis')
module_select = Transition(label='Module Select')
component_order = Transition(label='Component Order')
parts_inspect = Transition(label='Parts Inspect')
frame_assemble = Transition(label='Frame Assemble')
sensor_install = Transition(label='Sensor Install')
motor_attach = Transition(label='Motor Attach')
wiring_connect = Transition(label='Wiring Connect')
software_upload = Transition(label='Software Upload')
calibration_test = Transition(label='Calibration Test')
flight_simulate = Transition(label='Flight Simulate')
quality_review = Transition(label='Quality Review')
user_train = Transition(label='User Train')
remote_setup = Transition(label='Remote Setup')
feedback_collect = Transition(label='Feedback Collect')
support_schedule = Transition(label='Support Schedule')

xor1 = OperatorPOWL(operator=Operator.XOR, children=[spec_analysis, module_select])
xor2 = OperatorPOWL(operator=Operator.XOR, children=[parts_inspect, frame_assemble])
xor3 = OperatorPOWL(operator=Operator.XOR, children=[sensor_install, motor_attach])
xor4 = OperatorPOWL(operator=Operator.XOR, children=[wiring_connect, software_upload])
xor5 = OperatorPOWL(operator=Operator.XOR, children=[calibration_test, flight_simulate])
xor6 = OperatorPOWL(operator=Operator.XOR, children=[quality_review, user_train])
xor7 = OperatorPOWL(operator=Operator.XOR, children=[remote_setup, feedback_collect])
xor8 = OperatorPOWL(operator=Operator.XOR, children=[support_schedule])

root = StrictPartialOrder(nodes=[client_consult, xor1, xor2, xor3, xor4, xor5, xor6, xor7, xor8])
root.order.add_edge(client_consult, xor1)
root.order.add_edge(xor1, xor2)
root.order.add_edge(xor2, xor3)
root.order.add_edge(xor3, xor4)
root.order.add_edge(xor4, xor5)
root.order.add_edge(xor5, xor6)
root.order.add_edge(xor6, xor7)
root.order.add_edge(xor7, xor8)